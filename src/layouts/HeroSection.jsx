import React from "react";

import {
    HeroStart,
    HeroBody
} from "../components/Hero";

function HeroSection() {
    return (
        <HeroStart>
            <HeroBody>
                <h1> sup </h1>
            </HeroBody>
        </HeroStart>
    )
}

export default HeroSection;
